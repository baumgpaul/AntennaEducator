/**
 * MoM Solver feature — stub.
 *
 * Will contain MoM-specific UI components (surface mesh, basis
 * functions, RWG elements, etc.).  Implementation on a dedicated
 * feature branch.
 */
export {};
